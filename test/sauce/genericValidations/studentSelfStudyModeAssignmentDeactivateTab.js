// require('colors');
// 
// var dataUtil = require("../util/date-utility");
// var loginPage = require("../support/pages/login");
// var session = require("../support/setup/browser-session");
// var studyBitCreation = require("../support/pages/createStudyBit");
// var qaTestData =  require("../../../test_data/qa.json");
// var testData =  require("../../../test_data/data.json");
// 
// describe('4LTR (' +'Student'+') :: studentSelfStudyModeAssignmentDeactivateTab Started', function() {
    // var browser;
    // var allPassed = true;
    // var userType;
    // var product;
// 
    // before(function(done) {
        // browser = session.create(done);
        // userType= testData.termsAssociatedWithStudentUser[4];
        // product= testData.termsAssociatedWithStudentUser[6];
    // });
// 
    // afterEach(function(done) {
        // allPassed = allPassed && (this.currentTest.state === 'passed');  
        // done();
    // });
// 
    // after(function(done) {
        // session.close(allPassed,done);
    // });
//     
	 // it("1. Log in as Student",function(done){
        // loginPage.loginToApplication(userType,browser,done);
	  // });
// 	  
	 // it("2. Select a Product",function(done){
	 	// loginPage.selectAProduct(userType,product,browser,done);
	 // });
// 	
	 // it("3. Verify the assignment on navigation bar", function(done) {
                // browser
                	// .sleep(5000)
                    // .elementByXPathSelectorWhenReady("//li[contains(@class,'ng-scope ng-isolate-scope disabled')]",2000)
                    // .isDisplayed()
			        // .should.become(true)
                    // .sleep(3000)
                    // .nodeify(done);
	 // });
// });
